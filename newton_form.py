import numpy

def newton(x, y, degree):
    if degree > length(x) - 1:
        return none
    a = []
    f = []
    a[0] = 1
    f[0] = 1;
    for i in range(1, n + 1):
        




def eval_newton(f, var):
